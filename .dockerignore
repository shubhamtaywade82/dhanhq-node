# Never bake secrets or local state into the image
.env
.env.*

# VCS & CI
.git
.github

# Dependencies (installed inside the image by the Dockerfile)
node_modules
frontend/node_modules

# Build outputs (recompiled inside the image)
dist
frontend/dist

# Local artifacts
*.log
coverage
.DS_Store
